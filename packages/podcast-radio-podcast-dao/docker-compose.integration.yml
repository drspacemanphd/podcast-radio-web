version: '3.8'
services:
  localstack:
    container_name: "${LOCALSTACK_DOCKER_NAME-localstack_main}"
    image: localstack/localstack
    ports:
      - "4566:4566"
    environment:
      - LOCALSTACK_SERVICES=dynamodb
    networks: 
      podcast-radio-podcast-dao-integration:
  integration_tests:
    build: 
      dockerfile: ./Dockerfile.integration.tests
      context: ./
    ports: 
      - "9000:9000"
    volumes: 
      - ./:/app/
      - ../../node_modules:/app/node_modules # neccessary to bring in top-level dependencies for local dev
      - ../../packages:/app/packages # necessary only when referencing local non-published packages for local dev
      - ~/.aws:/root/.aws
    depends_on: 
      - localstack
    command: jest --globalSetup ./tests/integration/setup.js --testMatch=**/integration/**/*.test.ts --maxConcurrency=0 --detectOpenHandles --verbose
    env_file: 
      - ./.env.local
    networks: 
      podcast-radio-podcast-dao-integration:

networks: 
  podcast-radio-podcast-dao-integration: