{
  "name": "podcast-radio-web",
  "version": "0.0.1-alpha.0",
  "description": "A Podcast Radio Application Mono-repo",
  "repository": "git@github.com:drspacemanphd/podcast-radio-web.git",
  "author": "drspacemanphd",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.1.2"
  },
  "scripts": {
    "local": "docker-compose -f ./docker-compose.local.yml up -d --build",
    "deploy": "lerna version --yes && lerna run build && lerna publish from-package --yes --contents dist"
  }
}
